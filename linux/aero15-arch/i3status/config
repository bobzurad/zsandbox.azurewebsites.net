# i3status configuration file.
# see "man i3status" for documentation.

# It is important that this file is edited as UTF-8.
# The following line should contain a sharp s:
# ß
# If the above line is not correctly displayed, fix your editor first!

general {
	colors = true
	color_good = '#42b3d8'
	color_degraded = '#df882b'
	color_bad = '#f61920'
	interval = 2
}

# order += "ipv6"
order += "cpu_usage"
order += "cpu_temperature 0"
order += "memory"
order += "ethernet _first_"
order += "wireless _first_"
order += "disk /"
order += "disk /mnt/sda1"
order += "battery all"
order += "tztime local"
order += "volume master"

wireless _first_ {
        format_up = "📡: %quality %bitrate %frequency %essid %ip"
        format_down = "📡: down"
}

ethernet _first_ {
        format_up = "E: %ip (%speed)"
        format_down = "E: down"
}

battery all {
        format = "⚡ %status %percentage %remaining"
}

disk "/" {
        format = "%avail"
	low_threshold = 2
	threshold_type = "gbytes_avail"
	prefix_type = "binary"
}

disk "/mnt/sda1" {
	format = "%avail"
	low_threshold = 2
	threshold_type = "gbytes_avail"
	prefix_type = "binary"
}

cpu_temperature 0 {
        format = "T: %degrees °C"
        path = "/sys/devices/platform/coretemp.0/hwmon/hwmon*/temp1_input"
}

cpu_usage {
	format = "%usage"
	max_threshold = 80
}

load {
        format = "%1min"
}

memory {
        format = "%used | %available"
        threshold_degraded = "1G"
        format_degraded = "MEMORY < %available"
}

tztime local {
        format = "%Y-%b-%d %I:%M %p"
}

volume master {
	device = "pulse"
	format = "🔉 %volume"
	format_muted = "🔇 %volume"
}

